rules_version = '2';

// Placeholder - las reglas de seguridad se definirán en la Fase A de la migración.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}